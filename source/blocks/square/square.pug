
mixin square(parameters)
	- const { color, text, opacity } = parameters

	.square
		div(style='background: ' + color + '; opacity: ' + opacity).square__color
		.square__wrapper
			p.square__text #{text}
			p.square__text-color #{color}

