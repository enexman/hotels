include ../main-nav/main-nav.pug

mixin header(parameters)
	- var items = parameters.items
	- var user = parameters.user
	- var ui = parameters.ui

	header.header
		.header__wrapper
			include ../logo/logo.pug
			+main-nav({items: items, user: user, ui: ui})


