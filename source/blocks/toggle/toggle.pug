mixin toggle(parameters)
	- const { text, checked } = parameters

	div.toggle
		label.toggle__label
			input(type='checkbox' checked=checked).toggle__input
			span.toggle__box
			span.toggle__circle
			span.toggle__text #{text}
